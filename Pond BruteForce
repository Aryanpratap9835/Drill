#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int n,k;
	cin>>n>>k;
	vector<vector<int>>ans(n,vector<int>(n));
	for(int i=0;i<n;i++){
	    for(int j=0;j<n;j++){
	        cin>>ans[i][j];
	    }
	}
		int size=(k*k)/2 +1;
		int answer=1e9;
	for(int i=0;i<=n-k;i++){
	    for(int j=0;j<=n-k;j++){
	        	int nas=1e9;
	        		vector<int>kas;
	        for(int x=i;x<i+k;x++){
	            for(int y=j;y<j+k;y++){
	               kas.push_back(ans[x][y]);
	            }
	        }
	        sort(kas.rbegin(),kas.rend());
	        int x=kas[size-1];
	        answer=min(answer,x);
	       
	    }
	}
	cout<<answer<<"\n";

}
